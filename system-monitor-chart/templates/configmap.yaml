apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "system-monitor-chart.fullname" . }}-config
  labels:
    {{- include "system-monitor-chart.labels" . | nindent 4 }}
data:
  LOG_FILE: {{ .Values.config.logFile | quote }}
  THRESHOLD: {{ .Values.config.threshold | quote }}
