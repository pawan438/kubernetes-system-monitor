apiVersion: v1
kind: ServiceAccount
metadata:
  name: system-monitor-sa        # Dedicated identity for system-monitor pods
                                  # instead of using the "default" ServiceAccount,
                                  # which is shared by everything in the namespace
                                  # with no permissions boundary of its own.
