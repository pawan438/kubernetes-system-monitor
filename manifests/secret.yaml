apiVersion: v1                              # Core API version
kind: Secret                                # Sensitive data storage
metadata:
  name: system-monitor-secret               # Name of the Secret
type: Opaque                                # Generic key-value secret
stringData:                                 # Plain text here; k8s encodes it for us
  ALERT_WEBHOOK_TOKEN: "changeme-token-123" # Placeholder token for alert notifications
