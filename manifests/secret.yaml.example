apiVersion: v1
kind: Secret
metadata:
  name: system-monitor-secret
type: Opaque
stringData:
  ALERT_WEBHOOK_TOKEN: "REPLACE_ME"   # Set your real webhook token here.
                                       # This file (secret.yaml.example) is safe to commit.
                                       # The real secret.yaml is gitignored — never commit it.
