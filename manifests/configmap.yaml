apiVersion: v1                          # Core API version
kind: ConfigMap                         # Non-sensitive config storage
metadata:
  name: system-monitor-config           # Name of the ConfigMap
data:
  LOG_FILE: "/app/data/health.log"      # Path where health checks get logged
  THRESHOLD: "85"                       # CPU/RAM/Disk warning threshold
